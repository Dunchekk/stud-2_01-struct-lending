# Where are we? — лонгрид об искусственном интеллекте. Студенческий проект.

👉 Read: https://dunchekk.github.io/stud-2_01-struct-lending/

### Описание

> Искусственный интеллект — технология, которая вмешивается в ткань нашей жизни и расшатывает устоявшиеся понятия и концепции. Это слишком активный новый актор, игнорировать который становится все труднее.
>
> Where are we? — ризоматический лонгрид о развитии ии в методологии постструктурализма. Попытка собрать знакомое и всё ещё актуальное, чтобы твёрже стоять на теоретической почве нового мира.

Основное тело лонгрида состоит из 39 узлов-нодов, которые при каждой загрузке в случайном порядке генерации получают свои новые координаты на поле — так при перезагрузке страницы вы никогда не получите тот же лонгрид, что видели раньше.

Подробнее про идею: https://hsedesign.ru/project/68c8e820ce34431f8b13106c561fac78

### Стек

- `Vite` — dev-сервер и сборка.
- `PixiJS` (через CDN) — фоновая графика/видео и эффекты.
- `Vanilla JS` (ESM) — логика нодов, оверлеев и взаимодействий.
- `CSS` — анимации, адаптивы, blending-эффекты.

### Структура

```text
src/
├── main.js                     — точка входа: подключает CSS и включает все модули (pixi/поле/обработчики/лоадер).
├── components/
│   ├── activeDragging.js       — drag-to-scroll для `#main-field-wrapper` (перетаскивание мышью вместо скролла).
│   ├── imgControl.js           — «плавающие» изображения: спавн/анимация/остановка.
│   ├── initMainField.js        — инициализация поля: загрузка данных (JSON), расчёт размеров, раскладка и рендер нодов, resize.
│   ├── introField.js           — логика init-оверлея — `#inition-field`: текстовый блок, листание, закрытие.
│   ├── initUI.js               — синхронизация UI при открытии/закрытии init-оверлея.
│   ├── loading.js              — скрытие загрузочного экрана, когда поле+pixi+медиа готовы (с таймаут-фоллбеком).
│   ├── observer.js             — IntersectionObserver для `.map-item` (reveal/in-view) + одноразовая центровка поля.
│   ├── pixi.js                 — PixiJS фон: видео-текстура, blur/mask (на десктопе), параллакс по скроллу поля.
│   ├── questionField.js        — оверлей «объяснения» (`#blending-explenation`): открыть/закрыть, анимации, low-perf режим.
│   └── renderers.js            — рендер нодов: картинки/видео + hover-эффекты, текстовые карточки + листание колесом/свайпом.
└── css/
    ├── adaptives.css           — адаптивные стили.
    ├── blending-modes.css      — стили оверлеев (questions/init), утилиты анимаций, floating images, элементы UI.
    ├── eges.css                — «края» (edge overlays): градиенты/blur сверху/снизу/слева/справа.
    ├── font.css                — шрифт (spectral) и базовая глобальная типографика.
    ├── keyframes.css           — keyframes для fade/invert анимаций (blendFadeIn/Out, htmlInverseIn/Out).
    ├── nodes.css               — стили нодов (`.text-el`, `.pic-el`, `.map-item`), reveal-анимация, low-perf упрощения.
    ├── pixi.css                — обвязка Pixi-контейнера и классы для анимации инверсии.
    └── style.css               — базовая раскладка: скрытие скроллбаров, размеры/скролл полей, курсор.
```
### Скриншоты

![Screenshot 1](public/media/screenshots/1.png)

![Screenshot 3](public/media/screenshots/3.png)

![Screenshot 2](public/media/screenshots/2.png)
